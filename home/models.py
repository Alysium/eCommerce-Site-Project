from django.db import models

# Create your models here.

#need to create users storage, with username, password !DJANGO ALREADY HAS ABILITY TO CREATE USERS



#class category(models.Model):
	#dId = models.AutoField(primary_key=True)
#	cName = models.CharField(max_length=200)

	
#class store(models.Model):
	#sId = models.AutoField(primary_key=True)
#	sCategory = models.ForeignKey(category, on_delete=models.CASCADE)
#	sName = models.CharField(max_length=200)
#	sLogo = models.CharField(max_length=1000) #to be changed to actual picture of each store

#class item(models.Model):
#	iCategory = models.ForeignKey(store, on_delete=models.CASCADE)
#	iName = models.CharField(max_length=200)
#	iPicture = models.CharField(max_length=1000) #to be changed to actual picture of each item, different angles
#	iPrice = models.BigIntegerFIeld
	
	




